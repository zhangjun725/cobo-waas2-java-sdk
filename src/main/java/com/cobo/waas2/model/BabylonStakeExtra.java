/*
 * Cobo Wallet as a Service 2.0
 *
 * Contact: support@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.cobo.waas2.model;

import java.util.Objects;
import com.cobo.waas2.model.StakingPoolType;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.cobo.waas2.JSON;

/**
 * BabylonStakeExtra
 */
@javax.annotation.Generated(
    value = "org.openapitools.codegen.languages.JavaClientCodegen", 
    comments = "Generator version: 7.6.0"
)
public class BabylonStakeExtra {
  public static final String SERIALIZED_NAME_POOL_TYPE = "pool_type";
  @SerializedName(SERIALIZED_NAME_POOL_TYPE)
  private StakingPoolType poolType;

  public static final String SERIALIZED_NAME_FINALITY_PROVIDER_PUBLIC_KEY = "finality_provider_public_key";
  @SerializedName(SERIALIZED_NAME_FINALITY_PROVIDER_PUBLIC_KEY)
  private String finalityProviderPublicKey;

  public static final String SERIALIZED_NAME_STAKE_BLOCK_TIME = "stake_block_time";
  @SerializedName(SERIALIZED_NAME_STAKE_BLOCK_TIME)
  private Long stakeBlockTime;

  public static final String SERIALIZED_NAME_ONLY_SIGN = "only_sign";
  @SerializedName(SERIALIZED_NAME_ONLY_SIGN)
  private Boolean onlySign;

  public BabylonStakeExtra() {
  }

  public BabylonStakeExtra poolType(StakingPoolType poolType) {
    this.poolType = poolType;
    return this;
  }

   /**
   * Get poolType
   * @return poolType
  **/
  @javax.annotation.Nonnull
  public StakingPoolType getPoolType() {
    return poolType;
  }

  public void setPoolType(StakingPoolType poolType) {
    this.poolType = poolType;
  }


  public BabylonStakeExtra finalityProviderPublicKey(String finalityProviderPublicKey) {
    this.finalityProviderPublicKey = finalityProviderPublicKey;
    return this;
  }

   /**
   * The public key of finality provider.
   * @return finalityProviderPublicKey
  **/
  @javax.annotation.Nonnull
  public String getFinalityProviderPublicKey() {
    return finalityProviderPublicKey;
  }

  public void setFinalityProviderPublicKey(String finalityProviderPublicKey) {
    this.finalityProviderPublicKey = finalityProviderPublicKey;
  }


  public BabylonStakeExtra stakeBlockTime(Long stakeBlockTime) {
    this.stakeBlockTime = stakeBlockTime;
    return this;
  }

   /**
   * The stake block time.
   * @return stakeBlockTime
  **/
  @javax.annotation.Nonnull
  public Long getStakeBlockTime() {
    return stakeBlockTime;
  }

  public void setStakeBlockTime(Long stakeBlockTime) {
    this.stakeBlockTime = stakeBlockTime;
  }


  public BabylonStakeExtra onlySign(Boolean onlySign) {
    this.onlySign = onlySign;
    return this;
  }

   /**
   * Whether to only sign transactions. Default is &#x60;false&#x60;, if set to &#x60;true&#x60;,  the transaction will not be submitted to the blockchain automatically. You can call &#x60;Broadcast transactions&#x60; to submit the transaction to the blockchain,  Or you can find the signed raw_tx by &#x60;Get transaction information&#x60; and broadcast it yourself. 
   * @return onlySign
  **/
  @javax.annotation.Nullable
  public Boolean getOnlySign() {
    return onlySign;
  }

  public void setOnlySign(Boolean onlySign) {
    this.onlySign = onlySign;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the BabylonStakeExtra instance itself
   */
  public BabylonStakeExtra putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    BabylonStakeExtra babylonStakeExtra = (BabylonStakeExtra) o;
    return Objects.equals(this.poolType, babylonStakeExtra.poolType) &&
        Objects.equals(this.finalityProviderPublicKey, babylonStakeExtra.finalityProviderPublicKey) &&
        Objects.equals(this.stakeBlockTime, babylonStakeExtra.stakeBlockTime) &&
        Objects.equals(this.onlySign, babylonStakeExtra.onlySign)&&
        Objects.equals(this.additionalProperties, babylonStakeExtra.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(poolType, finalityProviderPublicKey, stakeBlockTime, onlySign, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class BabylonStakeExtra {\n");
    sb.append("    poolType: ").append(toIndentedString(poolType)).append("\n");
    sb.append("    finalityProviderPublicKey: ").append(toIndentedString(finalityProviderPublicKey)).append("\n");
    sb.append("    stakeBlockTime: ").append(toIndentedString(stakeBlockTime)).append("\n");
    sb.append("    onlySign: ").append(toIndentedString(onlySign)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("pool_type");
    openapiFields.add("finality_provider_public_key");
    openapiFields.add("stake_block_time");
    openapiFields.add("only_sign");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("pool_type");
    openapiRequiredFields.add("finality_provider_public_key");
    openapiRequiredFields.add("stake_block_time");
  }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to BabylonStakeExtra
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!BabylonStakeExtra.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in BabylonStakeExtra is not found in the empty JSON string", BabylonStakeExtra.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : BabylonStakeExtra.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the required field `pool_type`
      StakingPoolType.validateJsonElement(jsonObj.get("pool_type"));
      if (!jsonObj.get("finality_provider_public_key").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `finality_provider_public_key` to be a primitive type in the JSON string but got `%s`", jsonObj.get("finality_provider_public_key").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!BabylonStakeExtra.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'BabylonStakeExtra' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<BabylonStakeExtra> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(BabylonStakeExtra.class));

       return (TypeAdapter<T>) new TypeAdapter<BabylonStakeExtra>() {
           @Override
           public void write(JsonWriter out, BabylonStakeExtra value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public BabylonStakeExtra read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             BabylonStakeExtra instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of BabylonStakeExtra given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of BabylonStakeExtra
  * @throws IOException if the JSON string is invalid with respect to BabylonStakeExtra
  */
  public static BabylonStakeExtra fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, BabylonStakeExtra.class);
  }

 /**
  * Convert an instance of BabylonStakeExtra to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}


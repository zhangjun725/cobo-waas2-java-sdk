/*
 * Cobo Wallet as a Service 2.0
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.cobo.waas2.model;

import java.util.Objects;
import com.cobo.waas2.model.SafeWallet;
import com.cobo.waas2.model.SmartContractInitiator;
import com.cobo.waas2.model.SmartContractWalletType;
import com.cobo.waas2.model.WalletSubtype;
import com.cobo.waas2.model.WalletType;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.UUID;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.cobo.waas2.JSON;

@javax.annotation.Generated(
    value = "org.openapitools.codegen.languages.JavaClientCodegen", 
    comments = "Generator version: 7.6.0"
)
public class SmartContractWalletInfo extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(SmartContractWalletInfo.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!SmartContractWalletInfo.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'SmartContractWalletInfo' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<SafeWallet> adapterSafeWallet = gson.getDelegateAdapter(this, TypeToken.get(SafeWallet.class));

            return (TypeAdapter<T>) new TypeAdapter<SmartContractWalletInfo>() {
                @Override
                public void write(JsonWriter out, SmartContractWalletInfo value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `SafeWallet`
                    if (value.getActualInstance() instanceof SafeWallet) {
                        JsonElement element = adapterSafeWallet.toJsonTree((SafeWallet)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: SafeWallet");
                }

                @Override
                public SmartContractWalletInfo read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    JsonObject jsonObject = jsonElement.getAsJsonObject();

                    // use discriminator value for faster oneOf lookup
                    SmartContractWalletInfo newSmartContractWalletInfo = new SmartContractWalletInfo();
                    if (jsonObject.get("smart_contract_wallet_type") == null) {
                        log.log(Level.WARNING, "Failed to lookup discriminator value for SmartContractWalletInfo as `smart_contract_wallet_type` was not found in the payload or the payload is empty.");
                    } else  {
                        // look up the discriminator value in the field `smart_contract_wallet_type`
                        switch (jsonObject.get("smart_contract_wallet_type").getAsString()) {
                            case "Safe":
                                deserialized = adapterSafeWallet.fromJsonTree(jsonObject);
                                newSmartContractWalletInfo.setActualInstance(deserialized);
                                return newSmartContractWalletInfo;
                            case "SafeWallet":
                                deserialized = adapterSafeWallet.fromJsonTree(jsonObject);
                                newSmartContractWalletInfo.setActualInstance(deserialized);
                                return newSmartContractWalletInfo;
                            default:
                                log.log(Level.WARNING, String.format("Failed to lookup discriminator value `%s` for SmartContractWalletInfo. Possible values: Safe SafeWallet", jsonObject.get("smart_contract_wallet_type").getAsString()));
                        }
                    }

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize SafeWallet
                    try {
                        // validate the JSON object to see if any exception is thrown
                        SafeWallet.validateJsonElement(jsonElement);
                        actualAdapter = adapterSafeWallet;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'SafeWallet'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for SafeWallet failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'SafeWallet'", e);
                    }

                    if (match == 1) {
                        SmartContractWalletInfo ret = new SmartContractWalletInfo();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for SmartContractWalletInfo: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public SmartContractWalletInfo() {
        super("oneOf", Boolean.FALSE);
    }

    public SmartContractWalletInfo(SafeWallet o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("SafeWallet", SafeWallet.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return SmartContractWalletInfo.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * SafeWallet
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof SafeWallet) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be SafeWallet");
    }

    /**
     * Get the actual instance, which can be the following:
     * SafeWallet
     *
     * @return The actual instance (SafeWallet)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `SafeWallet`. If the actual instance is not `SafeWallet`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SafeWallet`
     * @throws ClassCastException if the instance is not `SafeWallet`
     */
    public SafeWallet getSafeWallet() throws ClassCastException {
        return (SafeWallet)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to SmartContractWalletInfo
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with SafeWallet
        try {
            SafeWallet.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for SafeWallet failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for SmartContractWalletInfo with oneOf schemas: SafeWallet. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of SmartContractWalletInfo given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of SmartContractWalletInfo
     * @throws IOException if the JSON string is invalid with respect to SmartContractWalletInfo
     */
    public static SmartContractWalletInfo fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, SmartContractWalletInfo.class);
    }

    /**
     * Convert an instance of SmartContractWalletInfo to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}


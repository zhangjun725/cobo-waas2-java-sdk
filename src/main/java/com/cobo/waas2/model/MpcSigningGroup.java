/*
 * Cobo Wallet as a Service 2.0
 *
 * The version of the OpenAPI document: 1.1.0
 * Contact: support@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.cobo.waas2.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.cobo.waas2.JSON;

/**
 * The information about the Signing Group of an MPC Wallet.
 */
@javax.annotation.Generated(
    value = "org.openapitools.codegen.languages.JavaClientCodegen", 
    comments = "Generator version: 7.6.0"
)
public class MpcSigningGroup {
  public static final String SERIALIZED_NAME_USED_KEY_SHARE_HOLDER_GROUP_ID = "used_key_share_holder_group_id";
  @SerializedName(SERIALIZED_NAME_USED_KEY_SHARE_HOLDER_GROUP_ID)
  private String usedKeyShareHolderGroupId;

  public static final String SERIALIZED_NAME_USED_TSS_NODE_IDS = "used_tss_node_ids";
  @SerializedName(SERIALIZED_NAME_USED_TSS_NODE_IDS)
  private List<String> usedTssNodeIds = new ArrayList<>();

  public MpcSigningGroup() {
  }

  public MpcSigningGroup usedKeyShareHolderGroupId(String usedKeyShareHolderGroupId) {
    this.usedKeyShareHolderGroupId = usedKeyShareHolderGroupId;
    return this;
  }

   /**
   * The ID of the Signing Group.
   * @return usedKeyShareHolderGroupId
  **/
  @javax.annotation.Nullable
  public String getUsedKeyShareHolderGroupId() {
    return usedKeyShareHolderGroupId;
  }

  public void setUsedKeyShareHolderGroupId(String usedKeyShareHolderGroupId) {
    this.usedKeyShareHolderGroupId = usedKeyShareHolderGroupId;
  }


  public MpcSigningGroup usedTssNodeIds(List<String> usedTssNodeIds) {
    this.usedTssNodeIds = usedTssNodeIds;
    return this;
  }

  public MpcSigningGroup addUsedTssNodeIdsItem(String usedTssNodeIdsItem) {
    if (this.usedTssNodeIds == null) {
      this.usedTssNodeIds = new ArrayList<>();
    }
    this.usedTssNodeIds.add(usedTssNodeIdsItem);
    return this;
  }

   /**
   * The ID of the TSS Nodes that are required to participate in the signature.
   * @return usedTssNodeIds
  **/
  @javax.annotation.Nullable
  public List<String> getUsedTssNodeIds() {
    return usedTssNodeIds;
  }

  public void setUsedTssNodeIds(List<String> usedTssNodeIds) {
    this.usedTssNodeIds = usedTssNodeIds;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the MpcSigningGroup instance itself
   */
  public MpcSigningGroup putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    MpcSigningGroup mpcSigningGroup = (MpcSigningGroup) o;
    return Objects.equals(this.usedKeyShareHolderGroupId, mpcSigningGroup.usedKeyShareHolderGroupId) &&
        Objects.equals(this.usedTssNodeIds, mpcSigningGroup.usedTssNodeIds)&&
        Objects.equals(this.additionalProperties, mpcSigningGroup.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(usedKeyShareHolderGroupId, usedTssNodeIds, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class MpcSigningGroup {\n");
    sb.append("    usedKeyShareHolderGroupId: ").append(toIndentedString(usedKeyShareHolderGroupId)).append("\n");
    sb.append("    usedTssNodeIds: ").append(toIndentedString(usedTssNodeIds)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("used_key_share_holder_group_id");
    openapiFields.add("used_tss_node_ids");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to MpcSigningGroup
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!MpcSigningGroup.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in MpcSigningGroup is not found in the empty JSON string", MpcSigningGroup.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("used_key_share_holder_group_id") != null && !jsonObj.get("used_key_share_holder_group_id").isJsonNull()) && !jsonObj.get("used_key_share_holder_group_id").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `used_key_share_holder_group_id` to be a primitive type in the JSON string but got `%s`", jsonObj.get("used_key_share_holder_group_id").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("used_tss_node_ids") != null && !jsonObj.get("used_tss_node_ids").isJsonNull() && !jsonObj.get("used_tss_node_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `used_tss_node_ids` to be an array in the JSON string but got `%s`", jsonObj.get("used_tss_node_ids").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!MpcSigningGroup.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'MpcSigningGroup' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<MpcSigningGroup> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(MpcSigningGroup.class));

       return (TypeAdapter<T>) new TypeAdapter<MpcSigningGroup>() {
           @Override
           public void write(JsonWriter out, MpcSigningGroup value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public MpcSigningGroup read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             MpcSigningGroup instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of MpcSigningGroup given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of MpcSigningGroup
  * @throws IOException if the JSON string is invalid with respect to MpcSigningGroup
  */
  public static MpcSigningGroup fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, MpcSigningGroup.class);
  }

 /**
  * Convert an instance of MpcSigningGroup to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

